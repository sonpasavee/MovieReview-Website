<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Detail</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="max-w-5xl mx-auto mt-8 p-6 bg-white rounded-2xl shadow-lg">
        <div class="flex flex-col lg:flex-row gap-6">

            <!-- Poster -->
            <img src="https://image.tmdb.org/t/p/w500<%= movieDetail.poster_path %>" class="w-full lg:w-1/3 rounded-lg">

            <!-- Movie Info -->
            <div class="flex-1">
                <h1 class="text-4xl font-bold mb-2">
                    <%= movieDetail.title %>
                </h1>
                <p class="text-gray-500 mb-2">ปี: <%= movieDetail.release_date.substring(0,4) %> | เรทติ้ง TMDb: <%=
                            movieDetail.vote_average %>/10</p>
                <p class="text-gray-700 mb-4">
                    <%= movieDetail.overview %>
                </p>

                <!-- Genres -->
                <% if(movieDetail.genres) { %>
                    <p class="text-gray-500 mb-4">
                        ประเภท:
                        <% movieDetail.genres.forEach(g=> { %>
                            <span class="inline-block bg-gray-200 px-2 py-1 rounded-full text-sm mr-2">
                                <%= g.name %>
                            </span>
                            <% }) %>
                    </p>
                    <% } %>

                        <!-- Favorite / Review Buttons -->
                        <% if(loggedIn){ %>
                            <form action="/movie/<%= movieDetail.id %>/review" method="POST" class="mb-4">
                                <textarea name="comment" required placeholder="เขียนรีวิวของคุณ..."
                                    class="w-full border rounded p-2 mb-2"></textarea>
                                <input type="number" name="rating" min="1" max="10" placeholder="ให้คะแนน (1-10)"
                                    class="w-24 border rounded p-1 mb-2">
                                <button type="submit"
                                    class="bg-indigo-600 text-white px-4 py-2 rounded hover:bg-indigo-700 transition">ส่งรีวิว</button>
                            </form>
                            <% } else { %>
                                <p class="text-gray-500">เข้าสู่ระบบเพื่อเขียนรีวิว</p>
                                <% } %>

            </div>
        </div>

        <!-- User Reviews -->
        <div class="mt-8">
            <h2 class="text-2xl font-bold mb-4">รีวิวจากผู้ใช้</h2>
            <% if(movieDetail.reviews && movieDetail.reviews.length> 0){ %>
                <% movieDetail.reviews.forEach(r=> { %>
                    <div class="border-b py-2">
                        <p class="text-gray-700"><strong>
                                <%= r.userName %>
                            </strong> ให้คะแนน: <%= r.rating %>/10</p>
                        <p class="text-gray-600">
                            <%= r.comment %>
                        </p>
                        <p class="text-gray-400 text-sm">
                            <%= r.createdAt.toDateString() %>
                        </p>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <p class="text-gray-500">ยังไม่มีรีวิว</p>
                            <% } %>
        </div>

        <a href="/" class="inline-block mt-6 text-indigo-600 hover:underline">🔙 กลับหน้าหลัก</a>
    </div>

</body>

</html>