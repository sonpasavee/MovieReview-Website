<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1.0" />
    <title>My Collections</title>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@400;700&family=Roboto:wght@400;700&display=swap"
        rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="/js/tailwind-config.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindplus/elements@1" type="module"></script>
</head>

<body class="font-sans text-sm text-white bg-gradient-to-b from-[#1F1F1F] via-[#121212] to-[#000000]">

    <%- include('partials/header') %>

    <div class="flex min-h-screen w-full">
        <main class="flex-1 py-8 px-4 sm:px-8">

            <h1 class="text-2xl sm:text-3xl font-bold mb-6">My Collections</h1>

            <% if (!collections || collections.length===0) { %>
            <div class="mt-6 rounded-xl border border-dashed border-gray-600 bg-black p-10 text-center">
                <p class="text-white font-medium">คุณยังไม่มี Collection</p>
                <p class="text-xs text-gray-400 mt-1">ลองสร้าง Collection ใหม่เพื่อเก็บหนังที่คุณชอบ</p>
            </div>
            <% } else { %>
            <% collections.forEach(c=> { %>
            <section class="mb-10 relative">

                <div class="flex justify-between items-center mb-4">
                    <h2 id="collection-name-<%= c._id %>"
                        class="text-xl sm:text-2xl font-semibold text-white">
                        <%= c.name %>
                    </h2>

                    <div class="relative inline-block text-left">
                        <button type="button" class="text-gray-400 hover:text-white font-bold text-lg"
                            id="menu-button-<%= c._id %>">⋮</button>

                        <div class="hidden absolute right-0 mt-2 w-36 origin-top-right bg-gray-900 border border-gray-700 rounded-md shadow-lg z-10"
                            id="dropdown-<%= c._id %>">
                            <div class="py-1">
                                <button onclick="openRenameModal('<%= c._id %>', '<%= c.name %>')"
                                    class="w-full text-left px-4 py-2 text-sm text-white hover:bg-gray-700">Rename</button>
                                <button command="show-modal" commandfor="delete-collection-<%= c._id %>"
                                    class="w-full text-left px-4 py-2 text-sm text-red-500 hover:bg-gray-700">Delete</button>
                            </div>
                        </div>
                    </div>
<<<<<<< HEAD
                </div>

                <% if (!c.movies || c.movies.length===0) { %>
                <p class="text-gray-400 mb-4">ไม่มีหนังใน Collection นี้</p>
                <% } else { %>
                <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-5">
                    <% c.movies.forEach(movie=> { %>
                    <div class="relative">
                        <a href="/movie/detail/<%= movie.movieId %>"
                            class="block rounded-xl overflow-hidden">
                            <img src="<%= movie.posterPath ? 'https://image.tmdb.org/t/p/w500' + movie.posterPath : '/images/no-poster.png' %>"
                                class="h-56 object-cover w-full" alt="<%= movie.title %>">
                        </a>

                        <!-- ปุ่มลบหนังจาก collection -->
                        <button command="show-modal"
                            commandfor="delete-movie-<%= c._id %>-<%= movie.movieId %>"
                            class="absolute top-2 right-2 rounded-full px-2 py-1 text-red-600 hover:text-red-400 font-bold text-lg">&times;</button>

                        <p class="mt-2 text-sm font-medium text-center text-white truncate">
                            <%= movie.title %>
                        </p>

                        <!-- Modal Confirm Delete Movie -->
                        <el-dialog>
                            <dialog id="delete-movie-<%= c._id %>-<%= movie.movieId %>"
                                aria-labelledby="dialog-title" class="fixed inset-0 size-auto max-h-none max-w-none overflow-y-auto bg-transparent backdrop:bg-transparent">
                                <el-dialog-backdrop class="fixed inset-0 bg-gray-500/75 transition-opacity data-closed:opacity-0 data-enter:duration-300 data-enter:ease-out data-leave:duration-200 data-leave:ease-in"></el-dialog-backdrop>
                                <div tabindex="0" class="flex min-h-full items-end justify-center p-4 text-center focus:outline-none sm:items-center sm:p-0">
                                    <el-dialog-panel class="relative transform overflow-hidden rounded-lg bg-white text-left shadow-xl transition-all data-closed:translate-y-4 data-closed:opacity-0 data-enter:duration-300 data-enter:ease-out data-leave:duration-200 data-leave:ease-in sm:my-8 sm:w-full sm:max-w-lg data-closed:sm:translate-y-0 data-closed:sm:scale-95">
                                        <div class="bg-white px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
                                            <div class="sm:flex sm:items-start">
                                                <div class="mx-auto flex size-12 shrink-0 items-center justify-center rounded-full bg-red-100 sm:mx-0 sm:size-10">
                                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" data-slot="icon" aria-hidden="true" class="size-6 text-red-600">
                                                        <path d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126ZM12 15.75h.007v.008H12v-.008Z" stroke-linecap="round" stroke-linejoin="round"/>
                                                    </svg>
                                                </div>
                                                <div class="mt-3 text-center sm:mt-0 sm:ml-4 sm:text-left">
                                                    <h3 id="dialog-title" class="text-base font-semibold text-gray-900">Delete Movie</h3>
                                                    <div class="mt-2">
                                                        <p class="text-sm text-gray-500">คุณต้องการลบหนังนี้จาก Collection หรือไม่?</p>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="bg-gray-50 px-4 py-3 sm:flex sm:flex-row-reverse sm:px-6">
                                            <form action="/collection/<%= c._id %>/remove/<%= movie.movieId %>" method="POST">
                                                <button type="submit" class="inline-flex w-full justify-center rounded-md bg-red-600 px-3 py-2 text-sm font-semibold text-white shadow-xs hover:bg-red-500 sm:ml-3 sm:w-auto">Delete</button>
                                            </form>
                                            <button type="button" command="close" commandfor="delete-movie-<%= c._id %>-<%= movie.movieId %>" class="mt-3 inline-flex w-full justify-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-xs inset-ring inset-ring-gray-300 hover:bg-gray-50 sm:mt-0 sm:w-auto">Cancel</button>
                                        </div>
                                    </el-dialog-panel>
                                </div>
                            </dialog>
                        </el-dialog>

                    </div>
                    <% }) %>
                </div>
                <% } %>

                <!-- Modal Confirm Delete Collection -->
                <el-dialog>
                    <dialog id="delete-collection-<%= c._id %>" aria-labelledby="dialog-title" class="fixed inset-0 size-auto max-h-none max-w-none overflow-y-auto bg-transparent backdrop:bg-transparent">
                        <el-dialog-backdrop class="fixed inset-0 bg-gray-500/75 transition-opacity data-closed:opacity-0 data-enter:duration-300 data-enter:ease-out data-leave:duration-200 data-leave:ease-in"></el-dialog-backdrop>
                        <div tabindex="0" class="flex min-h-full items-end justify-center p-4 text-center focus:outline-none sm:items-center sm:p-0">
                            <el-dialog-panel class="relative transform overflow-hidden rounded-lg bg-white text-left shadow-xl transition-all data-closed:translate-y-4 data-closed:opacity-0 data-enter:duration-300 data-enter:ease-out data-leave:duration-200 data-leave:ease-in sm:my-8 sm:w-full sm:max-w-lg data-closed:sm:translate-y-0 data-closed:sm:scale-95">
                                <div class="bg-white px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
                                    <div class="sm:flex sm:items-start">
                                        <div class="mx-auto flex size-12 shrink-0 items-center justify-center rounded-full bg-red-100 sm:mx-0 sm:size-10">
                                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" data-slot="icon" aria-hidden="true" class="size-6 text-red-600">
                                                <path d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126ZM12 15.75h.007v.008H12v-.008Z" stroke-linecap="round" stroke-linejoin="round"/>
                                            </svg>
                                        </div>
                                        <div class="mt-3 text-center sm:mt-0 sm:ml-4 sm:text-left">
                                            <h3 id="dialog-title" class="text-base font-semibold text-gray-900">Delete Collection</h3>
                                            <div class="mt-2">
                                                <p class="text-sm text-gray-500">คุณต้องการลบ Collection นี้จริงหรือไม่?</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="bg-gray-50 px-4 py-3 sm:flex sm:flex-row-reverse sm:px-6">
                                    <form action="/collection/delete/<%= c._id %>" method="POST">
                                        <button type="submit" class="inline-flex w-full justify-center rounded-md bg-red-600 px-3 py-2 text-sm font-semibold text-white shadow-xs hover:bg-red-500 sm:ml-3 sm:w-auto">Delete</button>
                                    </form>
                                    <button type="button" command="close" commandfor="delete-collection-<%= c._id %>" class="mt-3 inline-flex w-full justify-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-xs inset-ring inset-ring-gray-300 hover:bg-gray-50 sm:mt-0 sm:w-auto">Cancel</button>
                                </div>
                            </el-dialog-panel>
                        </div>
                    </dialog>
                </el-dialog>

            </section>
            <% }) %>
            <% } %>

        </main>
    </div>

    <div id="renameModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden z-50">
        <div class="bg-gray-900 rounded-lg p-6 w-80">
            <h3 class="text-lg font-semibold text-white mb-4">Rename Collection</h3>
            <input id="renameInput" type="text"
                class="w-full px-3 py-2 rounded bg-gray-800 text-white placeholder-gray-400 focus:outline-none" />
=======
                    <% } else { %>
                        <% collections.forEach(c=> { %>
                            <section class="mb-10 relative">
                                <<<<<<< HEAD=======>>>>>>> 02706dc (add colletion)
                                    <div class="flex justify-between items-center mb-4">
                                        <h2 id="collection-name-<%= c._id %>"
                                            class="text-xl sm:text-2xl font-semibold text-white">
                                            <%= c.name %>
                                        </h2>

                                        <div class="relative inline-block text-left">
                                            <button type="button"
                                                class="text-gray-400 hover:text-white font-bold text-lg"
                                                id="menu-button-<%= c._id %>">⋮</button>

                                            <div class="hidden absolute right-0 mt-2 w-36 origin-top-right bg-gray-900 border border-gray-700 rounded-md shadow-lg z-10"
                                                id="dropdown-<%= c._id %>">
                                                <div class="py-1">
                                                    <button onclick="openRenameModal('<%= c._id %>', '<%= c.name %>')"
                                                        class="w-full text-left px-4 py-2 text-sm text-white hover:bg-gray-700">Rename</button>
                                                    <form action="/collection/delete/<%= c._id %>" method="POST">
                                                        <button type="submit"
                                                            class="w-full text-left px-4 py-2 text-sm text-red-500 hover:bg-gray-700">Delete</button>
                                                    </form>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <% if (!c.movies || c.movies.length===0) { %>
                                        <p class="text-gray-400 mb-4">ไม่มีหนังใน Collection นี้</p>
                                        <% } else { %>
                                            <div
                                                class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-5">
                                                <% c.movies.forEach(movie=> { %>
                                                    <div class="relative">
                                                        <a href="/movie/detail/<%= movie.movieId %>"
                                                            class="block rounded-xl overflow-hidden">
                                                            <img src="<%= movie.posterPath ? 'https://image.tmdb.org/t/p/w500' + movie.posterPath : '/images/no-poster.png' %>"
                                                                class="h-56 object-cover w-full"
                                                                alt="<%= movie.title %>">
                                                        </a>

                                                        <!-- ปุ่มลบหนังจาก collection -->
                                                        <form
                                                            action="/collection/<%= c._id %>/remove/<%= movie.movieId %>"
                                                            method="POST" class="absolute top-2 right-2">
                                                            <button type="submit"
                                                                class="rounded-full px-2 py-1 text-red-600 hover:text-red-400 font-bold text-lg">&times;</button>
                                                        </form>

                                                        <p
                                                            class="mt-2 text-sm font-medium text-center text-white truncate">
                                                            <%= movie.title %>
                                                        </p>
                                                    </div>
                                                    <% }) %>
                                            </div>
                                            <% } %>
                            </section>
                            <% }) %>
                                <% } %>
>>>>>>> 9d0aeafb6c4e6fcb79489b8e16b58d8ed2f0de7b

            <div class="mt-4 flex justify-end gap-2">
                <button onclick="closeRenameModal()"
                    class="px-4 py-2 bg-gray-700 rounded text-white hover:bg-gray-600">Cancel</button>
                <button onclick="submitRename()"
                    class="px-4 py-2 bg-green-600 rounded text-white hover:bg-green-500">Save</button>
            </div>
        </div>
    </div>
<script>
            document.addEventListener('click', function (e) {
                document.querySelectorAll('[id^="dropdown-"]').forEach(dropdown => {
                    const menuButton = document.getElementById('menu-button-' + dropdown.id.split('-')[1]);
                    if (menuButton && menuButton.contains(e.target)) {
                        dropdown.classList.toggle('hidden');
                    } else if (!dropdown.contains(e.target)) {
                        dropdown.classList.add('hidden');
                    }
                });
            });

            let currentCollectionId = null;

            function openRenameModal(collectionId, collectionName) {
                currentCollectionId = collectionId;
                document.getElementById('renameInput').value = '';
                document.getElementById('renameInput').placeholder = collectionName;
                document.getElementById('renameModal').classList.remove('hidden');
            }

            function closeRenameModal() {
                document.getElementById('renameModal').classList.add('hidden');
            }

            function submitRename() {
                const newName = document.getElementById('renameInput').value.trim();
                if (!newName) return;

                fetch('/collection/rename/' + currentCollectionId, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ name: newName })
                })
                    .then(res => {
                        if (res.ok) {
                            document.querySelector(`#collection-name-${currentCollectionId}`).textContent = newName;
                            closeRenameModal();
                        } else {
                            alert('เกิดข้อผิดพลาด');
                        }
                    });
            }
        </script>
</body>
<<<<<<< HEAD
</html>
=======

</html>
>>>>>>> 9d0aeafb6c4e6fcb79489b8e16b58d8ed2f0de7b
