<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>รีวิวผู้ใช้ - Admin Dashboard</title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@400;700&display=swap" rel="stylesheet" />

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    body {
      font-family: "Kanit", sans-serif;
    }

    .glass-effect {
      background: rgba(20, 20, 20, 0.7);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .netflix-red {
      background-color: #e50914;
    }

    .card-hover:hover {
      transform: translateY(-2px);
      transition: all 0.2s ease-in-out;
    }
  </style>
</head>

<body class="bg-black text-white min-h-screen">
  <div class="max-w-7xl mx-auto px-6 py-8">
    <!-- Back Button -->
    <a href="/admin/dashboard" class="flex items-center text-red-400 hover:text-red-300 mb-8 transition-colors">
      <span class="mr-2">←</span> กลับไปแดชบอร์ด
    </a>

    <!-- Header -->
    <div class="glass-effect rounded-xl p-8 mb-8">
      <div class="flex items-center justify-between">
        <div class="flex items-center">
          <div class="w-20 h-20 rounded-full overflow-hidden mr-6">
            <% if (profile.avatarUrl && profile.avatarUrl.data) { %>
              <img
                src="data:<%= profile.avatarUrl.contentType %>;base64,<%= profile.avatarUrl.data.toString('base64') %>"
                alt="Avatar" class="w-full h-full object-cover" />
              <% } else { %>
                <div class="flex items-center justify-center w-full h-full bg-red-600 text-3xl font-bold text-white">
                  <%= user.name ? user.name.charAt(0).toUpperCase() : "?" %>
                </div>
                <% } %>
          </div>

          <div>
            <h1 class="text-3xl font-bold mb-2">
              <%= user.name %>
            </h1>
            <p class="text-gray-300 text-lg">
              <%= user.email %>
            </p>
            <p class="text-gray-400 text-sm">
              สมาชิกตั้งแต่:
              <%= new Date(user.createdAt).toLocaleDateString('th-TH', { day: 'numeric' , month: 'numeric' ,
                year: 'numeric' }) %>
            </p>
          </div>
        </div>
        <div class="bg-red-900 bg-opacity-60 rounded-lg p-5 text-center">
          <p class="text-gray-300 text-sm mb-1">รีวิวทั้งหมด</p>
          <p class="text-3xl font-bold text-white">
            <%= stats.totalReviews %>
          </p>
        </div>
      </div>
    </div>

    <!-- Stats -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
      <div class="rounded-xl p-6 glass-effect text-center card-hover transition">
        <div class="w-12 h-12 bg-yellow-600 bg-opacity-50 rounded-full flex items-center justify-center mx-auto mb-3">
          ⭐
        </div>
        <p class="text-gray-300 text-sm mb-1">คะแนนเฉลี่ย</p>
        <p class="text-2xl font-bold">
          <%= stats.avgRating %>
        </p>
      </div>

      <div class="rounded-xl p-6 glass-effect text-center card-hover transition">
        <div class="w-12 h-12 bg-green-600 bg-opacity-50 rounded-full flex items-center justify-center mx-auto mb-3">
          ✅
        </div>
        <p class="text-gray-300 text-sm mb-1">อนุมัติแล้ว</p>
        <p class="text-2xl font-bold">
          <%= stats.approvedReviews %>
        </p>
      </div>

      <div class="rounded-xl p-6 glass-effect text-center card-hover transition">
        <div class="w-12 h-12 bg-orange-600 bg-opacity-50 rounded-full flex items-center justify-center mx-auto mb-3">
          ⏳
        </div>
        <p class="text-gray-300 text-sm mb-1">รอการอนุมัติ</p>
        <p class="text-2xl font-bold">
          <%= stats.pendingReviews %>
        </p>
      </div>

      <div class="rounded-xl p-6 glass-effect text-center card-hover transition">
        <div class="w-12 h-12 bg-blue-600 bg-opacity-50 rounded-full flex items-center justify-center mx-auto mb-3">
          📅
        </div>
        <p class="text-gray-300 text-sm mb-1">รีวิวล่าสุด</p>
        <p class="text-lg font-bold">
          <%= stats.latestReviewDate %>
        </p>
      </div>
    </div>

    <!-- Reviews -->
    <div class="glass-effect rounded-xl p-8">
      <div class="flex items-center justify-between mb-6">
        <h2 class="text-2xl font-semibold flex items-center">
          📝 รีวิวทั้งหมดของผู้ใช้
        </h2>
        <div class="flex space-x-3">
          <form method="GET" id="filterForm" class="flex space-x-3">
            <input type="hidden" name="sort" value="<%= sort %>" />
            <select name="filter"
              class="px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg text-white focus:ring-2 focus:ring-red-500"
              onchange="document.getElementById('filterForm').submit()">
              <option value="all" <%=filter==='all' ? 'selected' : '' %>>ทั้งหมด</option>
              <option value="approved" <%=filter==='approved' ? 'selected' : '' %>>อนุมัติแล้ว</option>
              <option value="pending" <%=filter==='pending' ? 'selected' : '' %>>รอการอนุมัติ</option>
              <option value="rejected" <%=filter==='rejected' ? 'selected' : '' %>>ถูกปฏิเสธ</option>
            </select>

          </form>
        </div>

      </div>

      <div id="reviewsList">
        <% if (reviews.length> 0) { %>
          <% reviews.forEach(r=> { %>
            <div class="bg-gray-800 rounded-lg p-6 mb-5">
              <div class="flex justify-between items-center mb-2">
                <div class="review">
                  <img src="<%= r.movie.posterPath %>" alt="<%= r.movie.title %>" class="w-16 h-24" />
                </div>
                <div>
                  <p class="font-medium text-white">
                    <%= r.movie.title %>
                  </p>
                  <p class="text-sm text-gray-400">Movie Review</p>
                </div>
                <div>
                  <h3 class="text-xl font-semibold text-white mb-1">
                    <%= r.movieTitle %>
                  </h3>
                  <div class="flex items-center text-yellow-400 text-lg mb-1">
                    <% for (let i=0; i < r.rating; i++) { %>⭐<% } %>
                  </div>
                  <p class="text-gray-300">
                    <%= r.comment %>
                  </p>
                </div>
                <div class="text-right">
                  <p class="text-sm text-gray-400 mb-1">
                    <%= new Date(r.createdAt).toLocaleDateString('th-TH', { day: 'numeric' , month: 'numeric' ,
                      year: 'numeric' }) %>
                  </p>
                  <% if (r.status==='approved' ) { %>
                    <span class="bg-green-700 text-white text-sm px-3 py-1 rounded-lg">อนุมัติแล้ว</span>
                    <% } else if (r.status==='pending' ) { %>
                      <span class="bg-orange-600 text-white text-sm px-3 py-1 rounded-lg">รอการอนุมัติ</span>
                      <% } else { %>
                        <span class="bg-gray-600 text-white text-sm px-3 py-1 rounded-lg">ลบแล้ว</span>
                        <% } %>
                </div>
              </div>
            </div>
            <% }) %>
              <% } else { %>
                <div class="text-center py-12">
                  <div class="text-6xl mb-4">📝</div>
                  <p class="text-gray-400 text-lg">ไม่พบรีวิวของผู้ใช้นี้</p>
                </div>
                <% } %>
      </div>
    </div>
  </div>
</body>

</html>