<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>สมัครสมาชิก - เว็บรีวิวหนัง</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="flex items-center justify-center min-h-screen bg-cover bg-center font-sans"
    style="background-image: url('/images/login.png');">

    <div class="w-full max-w-lg bg-black/75 p-10 rounded-2xl shadow-xl">
        <!-- Title -->
        <h2 class="text-4xl font-bold text-white mb-3 text-left">สมัครสมาชิก</h2>
        <p class="text-gray-300 mb-8 text-left text-base">สร้างบัญชีเพื่อเริ่มต้นแชร์และอ่านรีวิวหนัง</p>

        <!-- Register Form -->
        <form action="/user/register" method="POST" class="space-y-6">
            <% if(errors !=null && errors.length> 0) { %>
                <% for(let i=0 ; i < errors.length ; i++) { %>
                    <p class="bg-red-600 text-white px-4 py-3 rounded text-sm" role="alert">
                        <%= errors[i] %>
                    </p>
                    <% } %>
                        <% } %>

                            <!-- Name -->
                            <div>
                                <input type="text" id="name" name="name" value="<%= name %>" required
                                    placeholder="ชื่อ-สกุล"
                                    class="w-full px-5 py-4 text-white bg-white/5 border border-white/40 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-600 placeholder-gray-300 text-lg">
                            </div>

                            <!-- Email -->
                            <div>
                                <input type="email" id="email" name="email" value="<%= email %>" required
                                    placeholder="อีเมล"
                                    class="w-full px-5 py-4 text-white bg-white/5 border border-white/40 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-600 placeholder-gray-300 text-lg">
                            </div>

                            <!-- Password -->
                            <div>
                                <input type="password" id="password" name="password" value="<%= password %>" required
                                    placeholder="รหัสผ่าน"
                                    class="w-full px-5 py-4 text-white bg-white/5 border border-white/40 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-600 placeholder-gray-300 text-lg">
                            </div>

                            <!-- Role -->
                            <div>
                                <select id="role" name="role" required class="w-full px-5 py-4 text-white 
               bg-white/5 border border-transparent rounded-lg 
               focus:outline-none focus:ring-2 focus:ring-red-600 text-lg 
               hover:border-white transition">
                                    <option value="user">ผู้ใช้ทั่วไป</option>
                                    <option value="admin">แอดมิน</option>
                                </select>
                            </div>


                            <!-- Button -->
                            <button type="submit"
                                class="w-full bg-red-600 hover:bg-red-700 text-white py-4 rounded-lg font-semibold text-center text-lg transition">
                                สมัครสมาชิก
                            </button>
        </form>

        <!-- Login link -->
        <p class="text-gray-300 text-center mt-8 text-base">
            มีบัญชีแล้ว?
            <a href="/login" class="text-white font-medium hover:underline">เข้าสู่ระบบ</a>
        </p>
    </div>

</body>

</html>