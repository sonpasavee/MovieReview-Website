<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= movie?.title || 'Admin • Detail' %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-b from-[#1a0000] via-[#0b0b0b] to-black text-zinc-100 flex flex-col">

  <!-- แถบบนเล็ก ๆ (ไม่ใช้ header partial) -->
  <div class="w-full bg-black/80 border-b border-red-900/40">
    <div class="max-w-6xl mx-auto px-4 py-3">
      <div class="text-lg font-semibold tracking-wide">R-MOVIE • Admin</div>
    </div>
  </div>

  <main class="flex-1 w-full">
    <div class="max-w-6xl mx-auto px-4 py-8">

      <!-- กล่องหลัก -->
      <div class="rounded-2xl border border-red-900/40 bg-black/70 shadow-xl overflow-hidden">

        <!-- ปุ่มสลับหน้า -->
        <div class="px-4 pt-4">
          <div class="inline-flex rounded-md border border-red-900/60 bg-black/40 p-1">
            <a href="/admin/movie/<%= movie?.movieId || movie?.id %>/detail"
               class="px-4 py-2 text-sm rounded-md bg-red-900/30 ring-1 ring-red-800/60">Detail</a>
            <a href="/admin/movie/<%= movie?.movieId || movie?.id %>/reviews"
               class="px-4 py-2 text-sm rounded-md hover:bg-white/5 transition">Reviews</a>
          </div>
        </div>

        <!-- เนื้อหา -->
        <div class="p-4 md:p-6 space-y-6">
          <section>
            <div class="grid grid-cols-1 md:grid-cols-[220px,1fr] gap-6">
              <!-- โปสเตอร์ -->
              <div>
                <img
                  src="<%= movie?.poster_path || movie?.posterPath || '/image/login.png' %>"
                  alt="<%= movie?.title %>"
                  class="w-[220px] h-auto rounded-xl border border-zinc-800 object-cover mx-auto md:mx-0"
                />
              </div>

              <!-- ข้อมูลหลัก -->
              <div>
                <h1 class="text-2xl md:text-3xl font-bold"><%= movie?.title || '—' %></h1>

                <div class="mt-2 text-sm text-zinc-300 flex flex-wrap items-center gap-3">
                  <% if (movie?.release_date) { %>
                    <span>ปี: <%= new Date(movie.release_date).getFullYear() %></span>
                  <% } %>
                  <% if (movie?.runtime) { %>
                    <span>ความยาว: <%= movie.runtime %> นาที</span>
                  <% } %>

                  <!-- ✅ คะแนนจาก MongoDB / Movie.rating -->
                  <span>
                    คะแนนผู้ใช้:
                    <% if (typeof movie?.vote_average === 'number') { %>
                      <%= movie.vote_average.toFixed(1) %>/10
                      <% if (movie?.vote_count) { %>
                        (<%= movie.vote_count %> รีวิว)
                      <% } %>
                    <% } else { %>
                      — /10
                    <% } %>
                  </span>
                </div>

                <!-- หมวดหมู่ -->
                <% if (movie?.genres && movie.genres.length) { %>
                  <div class="mt-3 flex flex-wrap gap-2">
                    <% movie.genres.forEach(g => { %>
                      <span class="text-xs px-2 py-1 rounded-full bg-red-900/30 border border-red-800/50">
                        <%= (typeof g === 'string') ? g : (g.name || g) %>
                      </span>
                    <% }) %>
                  </div>
                <% } %>

                <!-- ✅ เรื่องย่อ (จาก TMDb เท่านั้น) -->
                <% if (movie?.overview) { %>
                  <p class="mt-4 text-zinc-200 leading-7"><%= movie.overview %></p>
                <% } %>
              </div>
            </div>

            <!-- ตัวอย่าง (ถ้ามี) -->
            <% if (movie?.trailer_embed) { %>
              <div class="mt-6">
                <h3 class="mb-2 text-base text-zinc-200">ตัวอย่างหนัง</h3>
                <div class="aspect-video w-full rounded-xl overflow-hidden border border-zinc-800 bg-black">
                  <iframe
                    src="<%= movie.trailer_embed %>"
                    title="Trailer"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen
                    class="w-full h-full">
                  </iframe>
                </div>
              </div>
            <% } %>
          </section>
        </div>
      </div>
    </div>
  </main>
</body>
</html>
