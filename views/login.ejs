<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>เข้าสู่ระบบ - เว็บรีวิวหนัง</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="flex items-center justify-center min-h-screen bg-cover bg-center font-sans"
  style="background-image: url('/images/login.png');">

  <div class="w-full max-w-md bg-black/75 p-10 rounded-xl shadow-xl">
    <!-- Title -->
    <h2 class="text-4xl font-bold text-white mb-5 text-center">เข้าสู่ระบบ</h2>
    <p class="text-gray-300 text-center mb-8 text-base">เพื่อเริ่มต้นแชร์และอ่านรีวิวหนัง</p>

    <!-- Login Form -->
    <form action="/user/login" method="POST" class="space-y-6">
      <% if (messages.error) { %>
        <% messages.error.forEach(function(msg){ %>
          <p class="bg-red-600 text-white px-4 py-3 rounded text-sm" role="alert">
            <%= msg %>
          </p>
        <% }) %>
      <% } %>

      <!-- Email -->
      <div>
        <input type="email" id="email" name="email" required placeholder="อีเมล"
          class="w-full px-5 py-4 bg-white/5 text-white border border-white/40 rounded-sm focus:outline-none focus:ring-2 focus:ring-red-600 placeholder-gray-300 text-lg">
      </div>

      <!-- Password -->
      <div>
        <input type="password" id="password" name="password" required placeholder="รหัสผ่าน"
          class="w-full px-5 py-4 bg-white/5 text-white border border-white/40 rounded-sm focus:outline-none focus:ring-2 focus:ring-red-600 placeholder-gray-300 text-lg">
      </div>

      <!-- Button -->
      <button type="submit"
        class="w-full bg-red-600 hover:bg-red-700 text-white py-4 rounded-lg font-semibold text-lg transition">
        เข้าสู่ระบบ
      </button>
    </form>

    <!-- Register -->
    <p class="text-gray-400 text-center mt-8 text-base">
      ยังไม่มีบัญชี?
      <a href="/register" class="text-white font-medium hover:underline">สมัครสมาชิก</a>
    </p>
  </div>

</body>

</html>
